# The Master Object and Its Compatible Realizations

The master-object problem asks for a composition law that selects its positive evaluations, comparisons and admissible dynamics together. Its local realizations would supply the familiar objects called fields, clocks and particles; other realizations would supply global scale and cosmic response. The signature below organizes that search without declaring a category, state or Hamiltonian to be uniquely selected. The substantive theorem must restrict the realizations, not merely give them common notation.

## What counts as a primitive

Let \(\mathsf P\) be a proposed category of presentations and directed operations, with a wide subgroupoid \(\mathsf W\) of reversible presentation changes. Objects initially represent access conditions or boundary data. They are not assumed to be spacetime points. Comparison squares relate processing followed by readout to readout followed by the represented processing.

Each square must have a specified mathematical meaning: equality, a coherent isomorphism, or a measured defect. A double category or bicategory might organize these squares. A choice of that language alone does not supply the evaluations or select a process. [[program-core/operation-registers|Operations between registers]] distinguishes these maps, while [[algebra/algebra-before-geometry|algebra before geometry]] separates the initial doctrine from its realizations.

Nonassociative multiplication can occur within the data compared by such a structure. Its associator must have a declared role in the comparison squares. [[algebra/octonionic-associator-and-branch-forgetting|The octonionic analysis]] distinguishes that multiplication defect from information loss and chronology. The coherent composition of comparison operations can be associative even when the objects they compare carry nonassociative products.

A tentative signature is
\[
\mathfrak U=
(\mathsf P,\mathsf W,\mathsf{Src},\mathcal Z,\mathsf{Read},\mathscr S).
\tag{MO1}
\]
Here \(\mathsf{Src}\) assigns complete observable insertions to presentations; \(\mathcal Z\) evaluates sourced diagrams; \(\mathsf{Read}\) specifies their restriction or realization maps; and \(\mathscr S\) carries relative scale comparisons. Their compatibility is the intended law. The signature leaves open which entries are primitive and which are constructed.

## The full experiment is the unit of composition

A sourced diagram includes boundary variables, source products and the closed normalization. On a positive-kernel realization, sewing has the familiar expression
\[
K_{D_2\circ D_1}[J_1,J_2](x,z)
=\int K_{D_1}[J_1](x,y)K_{D_2}[J_2](y,z)\,d\mu(y).
\tag{MO2}
\]
This example already declares a measure and positivity. The foundational task is to explain their selection and their transport, not to present (MO2) as a definition valid before those choices exist.

[[conditional-preparation-sewing/inq|Conditional preparation sewing]] supplies exact members of this kind: parent-conditioned normalizers preserve the specified old marked experiment, while newly accessible sources can acquire correlated response. [[gauge-boundary-frame-gluing/inq|Boundary frame gluing]] explains why neutralizing each region separately can lose distinctions needed by the joint experiment.

Source transport must retain source products and their transported defects, including mixed sources supported on different parts. [[gauge-source-action-transport/joint-path-law-and-the-shared-boundary-action|The joint path law]] provides an actual compatibility condition. [[coarse-response-memory/product-boundary-frames-and-crossing-susceptibility|The crossing-susceptibility example]] proves that exact regional vacua still allow different joint responses. A list of local states is therefore insufficient data for (MO1).

## Evaluation supplies comparison geometry

In an associative realization, a positive normalized functional produces a GNS seminorm. Its null quotient and completion form a state carrier. A readout with the required state compatibility acts between those carriers. For a UCP readout, the product defect is the matrix-positive form in [[scale-bearing-descent/variance-completed-rigidity|variance-completed descent]].

This is a realization theorem after positivity and state evaluation exist. A universal object would need a principle selecting the admissible evaluations or constraining them across presentations. [[algebra/theorem-programme|The algebra-to-cosmos programme]] formulates possible representability questions. No particular functor, initial object or universal property has yet been proved to make the present master object unique.

The geometries must also keep their meanings. A purification covariance, a BKM state metric and an affine log-determinant Hessian are different constructions. [[algebra/purification-descent-and-the-matrix-response|Purification descent]] derives a law, response and generator from one Gram readout; [[program-core/common-response-form|the common response form]] specifies the maps needed to compare its own state tangents. Equality of their names or matrix dimensions is not a common-origin theorem.

## Scale is transported before it is calibrated

An additive valuation of directed arrows can represent accumulated comparison cost. A scale torsor represents relative calibration without selecting an origin. These need not initially be the same object. [[algebra/moving-response-balance-and-a-ruble-operator-signature|Moving response balance]] gives an additive determinant cost for its equal-rank invertible contractions; [[mass-scale-calibration/internal-yardstick-as-a-generalized-rate-edge|the generalized rate edge]] compares independently returned forms of the same process-density type.

A clock realization must commute with the appropriate access maps or retain their memory explicitly. [[algebra/partial-trace-clock-consistency-and-the-fluctuation-limit|Partial-trace consistency]] gives an exact common-clock example. [[algebra/occupation-conditioned-clocks-and-the-vacuum-boundary|Occupation-conditioned clocks]] shows why averaging clock rates can destroy autonomous evolution. Duration therefore enters through a compatibility theorem, not through an arbitrary scalar placed on the arrows of \(\mathsf P\).

The causal cone and local action require further structure. [[algebra/positive-cone-processes-and-the-complex-corner|The complex corner]] supplies a cone and conformal Lorentzian form after its flag is selected. [[algebra/cauchy-response-and-local-action|Opposed boundary response]] supplies complex and symplectic structures and a local action under its declared rotation prescription. The intended master object must construct a common input or comparison between these returns.

## A constitutive relation must remove an independent choice

Two tests distinguish selection from realization. First, hold all declared primitive data fixed and ask whether the alleged output can still be changed. Second, examine the primitive family itself: it may encode every desired output operator under another name. [[algebra/unbounded-projection-cycles-and-inherited-spectra|Closed-graph projection cycles]] gives a precise example of the second issue.

A stronger positive benchmark is [[primitive-state-diffusion/inq|primitive comparison refinement]], where a declared overlap law determines a full diffusion rather than receiving its Laplacian separately. [[general-causal-action/shared-preparation-state-and-mobility|Shared preparation]] ties stationary density, mobility and marked response to different moments of the same preparation. Their remaining orbit, preparation and normalization choices show what the next constitutive relation has to address.

For the gap, the target is an explicitly assembled repair of comparison errors, with a total loss budget that survives the admissible diagram operations. [[quantitative-descent/inq|Quantitative descent]] states the generic theorem; [[scale-bearing-descent/variance-completed-rigidity|variance-completed transport]] keeps the discarded analysis and repair channels. Its identification with the actual local innovation is a separate naturality requirement.

For a common cosmic and local origin, one source family must fix their variations together. In a fixed affine source chart, a genuinely common smooth potential obeys
\[
\partial_N\partial_i\partial_j\Psi
=\partial_i\partial_j\partial_N\Psi.
\tag{MO3}
\]
Commuting derivatives become informative only after the source maps, potential and fixed quantities are independently specified. [[general-causal-action/closed-normalization-and-cosmic-response|The marked determinant]] gives a finite example and exposes an additive scalar freedom that even mixed derivatives need not remove.

The sought discovery is a primitive relation that excludes this independent retuning while allowing an interacting, memory-bearing law. [[scale-bearing-descent/scale-symmetry-and-cosmic-response|Its different returns]] must then recover their own equations. The master object is the proposed reason those equations belong together, and its construction remains the central open problem.
