# The Yardstick Is a Rate, Not an Entropy

Entropy, logarithms, indices, and representation weights can supply dimensionless structure, but none is an energy yardstick. The immediate yardstick for a Hamiltonian gap is a physically normalized inverse time, converted by \(\hbar\), or an inverse length, converted by \(\hbar c\). Pure Yang--Mills already names its internal yardstick through dimensional transmutation, \(\Lambda_{\mathrm{YM}}\). A causal-cosmic construction could explain the realized value of that scale only through a separate matching and decoupling theorem.

**Status: [DIMENSIONAL NO-GO; CONDITIONAL YARDSTICK CONSTRUCTION].** The dimensional exclusions and algebraic rearrangements below are exact. The areal-information modulus, horizon KMS solder, event selector, and cross-sector RG matching remain open constructions.

## What each candidate can actually do

| Datum | Type | Legitimate role | Missing datum |
|---|---|---|---|
| \(\log W\), \(S/k_B\), nats, Misner \(N\), modular parameter | dimensionless | additive scale coordinate, count, cocycle, or entropy | a clock, ruler, or energy |
| \(A_2\) weights, Casimirs, indices, \(9/36\) | dimensionless | candidate coercivity or sector label | a dimensional normalization |
| Fisher/BKM metric | quadratic form on a declared tangent | distinguishability norm | a physical generator and clock |
| \(c\) | length per time | converts a supplied duration to length | selection of either duration or length |
| \(\hbar\) | energy times time | converts a supplied rate to energy | selection of the rate |
| \(k_B\) | energy per temperature | converts thermal entropy or temperature units | selection of a state and temperature |
| \(T_{\mathrm{KMS}}\) | temperature | fixes a clock solder when the KMS flow is physical | selection and normalization of that flow |
| \(H\) | inverse time | supplies the energy \(\hbar H\) | a reason this rate sets local Yang--Mills |
| \(G\) | gravitational response coefficient | forms Planck units with \(c,\hbar\) | a noncircular derivation and a gravity-to-Yang--Mills map |
| \(\chi=\mathrm d(S/k_B)/\mathrm dA\) | inverse area | supplies an areal information stiffness | an absolute spectral area independent of measured \(G\) |
| \(\Lambda_{\mathrm{YM}}\) | energy | native RG yardstick of pure Yang--Mills | a proof that the dimensionless gap coefficient is positive |

The central no-go is

$$
\boxed{
\text{dimensionless algebra}
+(c,\hbar,k_B)
\not\Longrightarrow
\text{a mass scale}.
}
$$

A length, duration, temperature, acceleration, or areal density must enter. Adding \(G\) supplies the Planck scale, but that imports a gravitational sector. Dimensional transmutation does not evade the rule: a reference scale and a dimensionless coupling are traded for the scheme-labelled invariant \(\Lambda_{\mathrm{YM}}\).

## The Clay factorization

Choose a renormalization scheme \(\mathsf s\), or replace the RG parameter below by a declared physical scale-setting observable. The mathematical problem should be attacked in the native form

$$
\boxed{
\Delta_H
=
\gamma_{\mathsf s}\Lambda_{\mathrm{YM}}^{(\mathsf s)},
\qquad
\gamma_{\mathsf s}>0.
}
$$

The two factors answer different questions:

- \(\Lambda_{\mathrm{YM}}^{(\mathsf s)}\) is the scheme-labelled quantum scale left by the obstruction to exact classical dilation symmetry;
- \(\gamma_{\mathsf s}\) is the corresponding dimensionless gap coefficient.

[[contemporary-puzzles/yang-mills-mass-gap/gauge-descent-flux-fisher-coercivity|Gauge-descended flux coercivity]] proves at finite regulator that

$$
c_{a,L}
:=
\frac{\Delta_{a,L}}{\kappa_a}
=
\inf_{f\perp1}
\frac{\mathcal E_{a,L}(f,f)}{\lVert f\rVert_2^2}.
$$

This identifies the *kind* of coercivity number required. It does not identify \(c_{a,L}\) with the continuum \(\gamma_{\mathsf s}\). The Clay-track target is a lower bound in \(\Lambda_{\mathrm{YM}}^{(\mathsf s)}\) units that is uniform in volume and regulator removal.

Scheme changes move the two factors oppositely. If

$$
\Lambda_{\mathrm{YM}}^{(\mathsf s')}
=
q\Lambda_{\mathrm{YM}}^{(\mathsf s)},
$$

then

$$
\gamma_{\mathsf s'}=q^{-1}\gamma_{\mathsf s}.
$$

Only their product is physical. A scheme-independent alternative is to normalize by an observable such as the string tension once its continuum existence has been constructed.

## Why Unruh temperature does not create a scale

For a physically normalized boost orbit with proper acceleration \(a\),

$$
k_BT_U
=
\frac{\hbar a}{2\pi c}.
$$

This is a conversion theorem. It says that the normalized acceleration \(a/c\) and the thermal rate \(2\pi k_BT_U/\hbar\) are two presentations of the same already selected clock. It does not select the observer, acceleration, horizon, or state.

The same firewall applies to modular flow. The Tomita--Takesaki parameter \(s\) is dimensionless. For a Gibbs/KMS realization,

$$
\sigma_s^\rho
=
\alpha_{-\beta\hbar s},
$$

up to sign convention. The factor \(\beta\hbar\) is the clock solder. Connes' outer flow can organize time representatives without supplying their duration in seconds.

## The strongest causal-cosmic yardstick

Suppose the wall construction independently produces a positive areal information modulus

$$
\boxed{
\chi_*
=
\frac{\mathrm d\mu_{\mathrm{BKM}}^\perp}
{\mathrm d\mu_{A,D}}
=
\frac{\mathrm d\mu_{\mathrm{edge}}}
{\mathrm d\mu_{A,D}}
>0,
}
$$

where \(\mu_{A,D}\) is an absolutely normalized spectral area measure obtained without measured \(G\). This is the theorem target of [[deriving-value-of-g/causal-scale-derivation-target|the causal-scale derivation of \(G\)]]. If \(\chi_*\) is instead read from the Bekenstein--Hawking law using measured \(G\), every formula below is only a translation of the input.

Suppose also that a constructed modular generator is proved to be the physically normalized horizon boost at a naturally selected cut, so that

$$
k_BT_c
=
\frac{\hbar c}{2\pi R_c}.
$$

This is explicitly open in [[causal-scale-theory/horizontal-temperature|the horizontal-temperature identification]].

The common-count relation

$$
\lambda_*^3
=
\frac83\ell_P^2R_c
$$

can be rewritten without using \(\ell_P\) as a primitive if the state--geometry weld proves

$$
\chi_*=\frac1{4\ell_P^2}.
$$

Then

$$
\lambda_*^3
=
\frac{2R_c}{3\chi_*}.
$$

For

$$
E_*:=\frac{\hbar c}{\lambda_*},
$$

the two independently typed wall data combine to give

$$
\boxed{
E_*^3
=
3\pi(\hbar c)^2\chi_*\,k_BT_c.
}
$$

Equivalently, define the geometric and thermal rates

$$
\omega_\chi:=c\sqrt{\chi_*},
\qquad
\omega_T:=\frac{k_BT_c}{\hbar}
=
\frac{c}{2\pi R_c}.
$$

Then

$$
\boxed{
\omega_*^3
=
3\pi\omega_\chi^2\omega_T,
\qquad
E_*=\hbar\omega_*.
}
$$

This rate form makes the conceptual ownership visible:

- \(\chi_*\) is local information stiffness per spectral area;
- \(k_BT_c\) is the energy conjugate to dimensionless entropy under the selected KMS normalization;
- \(c,\hbar,k_B\) convert presentations;
- the cube root comes from the three-dimensional common-count relation.

The entropy number itself is not the yardstick. Its density relative to independently normalized geometry and its rate relative to a selected clock are.

## A version with no measured \(G\) or \(H_c\) upstream

Suppose the algebra also selects a finite total wall count

$$
\mathcal I_c
:=
\frac{S_c}{k_B}
=
\chi_*A_c
$$

and spherical cut geometry gives

$$
A_c=4\pi R_c^2.
$$

Then

$$
R_c
=
\sqrt{\frac{\mathcal I_c}{4\pi\chi_*}},
$$

and the common-count relation yields

$$
\boxed{
E_*
=
\hbar c\,
(3\sqrt\pi)^{1/3}
\chi_*^{1/2}
\mathcal I_c^{-1/6}.
}
$$

In this order, \(G\) and, for a spatially flat FLRW realization whose apparent-horizon radius is \(R_c=c/H_c\), the distinguished Hubble rate would be simultaneous outputs,

$$
G_{\mathrm{pred}}
=
\frac{c^3}{4\hbar\chi_*},
\qquad
H_c=\frac c{R_c}.
$$

The formula is noncircular only if all of the following are constructed without those measured outputs:

1. the spectral area normalization;
2. the local density \(\chi_*\);
3. the total count \(\mathcal I_c\);
4. the spherical-cut realization; and
5. the state--geometry equality relating information density to gravitational response.

At present none of those five is complete on one physical carrier.

## Matching the cosmic yardstick to Yang--Mills

Even a noncircular \(E_*\) is not automatically \(\Lambda_{\mathrm{YM}}^{(\mathsf s)}\). The ontology track requires a new scheme-labelled dimensionless matching law,

$$
\boxed{
\Lambda_{\mathrm{YM}}^{(\mathsf s)}
=
C_{\mathsf s}E_*.
}
$$

The complete two-level factorization is therefore

$$
\boxed{
\frac{\Delta_H}{\hbar}
=
\gamma_{\mathsf s}\omega_{\mathrm{YM}}^{(\mathsf s)},
\qquad
\omega_{\mathrm{YM}}^{(\mathsf s)}
:=
\frac{\Lambda_{\mathrm{YM}}^{(\mathsf s)}}{\hbar}
=
C_{\mathsf s}\omega_*.
}
$$

Equivalently, in energy units,

$$
\boxed{
\Delta_H
=
\gamma_{\mathsf s}
C_{\mathsf s}
\left[
3\pi(\hbar c)^2\chi_*k_BT_c
\right]^{1/3}.
}
$$

Each factor has a different owner:

| Factor | Required theorem |
|---|---|
| \(\gamma_{\mathsf s}\) | uniform physical-carrier Poincare inequality in scheme \(\mathsf s\) |
| \(C_{\mathsf s}\) | cross-sector RG or anomaly matching in the same scheme |
| \(\chi_*\) | noncircular local state--area density |
| \(T_c\) | modular-to-proper-time solder at a selected wall |

No numerical success can replace any row.

Under a change \(\Lambda_{\mathsf s'}=q\Lambda_{\mathsf s}\), one has

$$
C_{\mathsf s'}=qC_{\mathsf s},
\qquad
\gamma_{\mathsf s'}=q^{-1}\gamma_{\mathsf s}.
$$

Thus the product

$$
\gamma_{\mathsf s}C_{\mathsf s}
=
\frac{\Delta_H}{E_*}
$$

is scheme-independent. A numerical value near thirty-six constrains this product, not either factor separately.

## Fossilization rather than live cosmic running

A dimensionally typed live identification would have the rate form

$$
\omega_{\mathrm{YM}}(t)
\propto
\left[
\omega_\chi(t)^2H(t)
\right]^{1/3}
$$

would make local hadronic scales evolve, vanish in the Minkowski limit, and conflict with the workspace's early-universe constancy checks. Renormalization-group invariance under changing subtraction scale is not conservation under cosmic time.

The viable type is instead a crossing-evaluated fossil. Let

$$
c:\mathsf{Sol}\longrightarrow\mathsf{Cut}
$$

be a presentation-natural event section. A central calibration observable is evaluated at \(c(\mathfrak s)\), transported into stable records, and assigned once to the whole solution \(\mathfrak s\). [[crossing-evaluated-flat-modulus/inq|The crossing-evaluated flat modulus]] proves that such a supplied scalar is constant along the cut fiber by type; it does not select the event or value.

A genuine fossilization theorem would require:

1. a natural event selector \(c\);
2. a central wall modulus preserved by record inclusions, a three-form constraint enforcing \(\mathrm d\eta=0\), or a top-form superselection sector;
3. an RG matching equation
   \[
   \Lambda_{\mathrm{YM}}^{(\mathsf s)}=C_{\mathsf s}E_c,
   \qquad
   \mu\frac{\mathrm d\Lambda_{\mathrm{YM}}^{(\mathsf s)}}{\mathrm d\mu}=0;
   \]
4. a local decoupling estimate, for regions of diameter \(r\),
   \[
   \left|
   \omega_{\mathrm{cosmic}}(\iota_c(O))
   -
   \omega_{\mathrm{YM}}(O)
   \right|
   \leq
   C\lVert O\rVert
   \left(\frac r{R_c}\right)^p,
   \]
   for bounded local \(O\) and a supplied comparison map \(\iota_c\), while the fossil \(\Lambda_{\mathrm{YM}}^{(\mathsf s)}\) remains fixed.

Flux, top forms, and record inclusions can preserve a value once supplied. They do not yet select its magnitude. This is the precise division of labor for fibers, flux, Connes time, descent, and causality.

## What would count as progress

The shortest serious programme is now:

1. prove the regulated flux--Poincare identity and use its Hellinger--Fisher realization to choose numerical and analytic observables;
2. establish a two-block or multiscale Poincare comparison on the gauge-invariant vacuum measure;
3. seek a constant uniform in volume and in \(\Lambda_{\mathrm{YM}}^{(\mathsf s)}\) units along the continuum trajectory;
4. separately construct \(\chi_*\), the modular clock solder, and the event selector without \(G\) or \(H_c\);
5. only then test a cross-sector matching \(C_{\mathsf s}\).

The first three steps address the mass-gap problem. The last two address why a cosmos realizes an absolute scale. Keeping them separate is not a retreat from the philosophical thesis; it is what makes the thesis mathematically falsifiable.
