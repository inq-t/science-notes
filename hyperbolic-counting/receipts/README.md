# Receipts

[[hyperbolic-counting/receipts/hyperbolic_counting.py|hyperbolic_counting.py]] (stdlib only, nonzero exit on failure) asserts: the orbifold Euler characteristics and areas including the least-area ordering; the nonzero four-decimal firewall gap between 5pi/6 and the golden index (the inequality itself is Hermite's theorem, not a float); the resolution depths and ledger values today, at matter--Lambda equality, and at the distinct programme crossing, with the identity iota = area/4lP^2; a finite-difference verification of d(ln iota)/dN = 2(1+q) in flat LCDM; the path-graph/Jones-ladder identity (A_{n-1} norm = 2cos(pi/n), with A3 of squared norm 2 rather than the type-I qubit model of index 4); the check that the displayed fit window lies above the affine threshold; a bounded search producing 22 candidate indices in the fit window (products of up to three ladder values with n from 3 to 24, plus the limit value 4 and selected integers 7 and 8), the named entries (golden squared, integer 7, 4+2sqrt3), the separation costs, and the nearest sampled half-log value (about 0.0016 from one; this finite sample proves neither density nor a gap near index e²); and the Gauss-map entropy constant. [[hyperbolic-counting/receipts/receipt-output.txt|receipt-output.txt]] is the run on this machine (2026-09-01).

A pass establishes the stated arithmetic and bounded search. It does not classify all finite-depth indices, establish the entropy–index equality or graph norm hypotheses in [[hyperbolic-counting/counting-rate-of-the-wall|the wall interpretation]], select a physical channel, or establish the radial reading of resolution depth. The stored output remains the historical run; its labels inherit these limits.
