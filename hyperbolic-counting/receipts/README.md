# Receipts

`hyperbolic_counting.py` (stdlib only, nonzero exit on failure) asserts: the orbifold Euler characteristics and areas including the least-area ordering; the nonzero four-decimal firewall gap between 5pi/6 and the golden index (the inequality itself is Hermite's theorem, not a float); the resolution depths and ledger values today, at matter--Lambda equality, and at the distinct programme crossing, with the identity iota = area/4lP^2; a finite-difference verification of d(ln iota)/dN = 2(1+q) in flat LCDM; the path-graph/Jones-ladder identity (A_{n-1} norm = 2cos(pi/n), with A3 of squared norm 2 rather than the type-I qubit model of index 4); the check that the displayed fit window lies above the affine threshold; the full enumeration of algebraic finite-depth survivor indices in the fit window (22 found), the named entries (golden squared, integer 7, 4+2sqrt3), the separation costs, and the accumulation bound (nearest survivor at 0.0016 nats from s* = 1 — transcendence is not decidable by digits); and the Gauss-map entropy constant. `receipt-output.txt` is the run on this machine (2026-09-01).

A pass establishes arithmetic and enumeration only — not the channel reading that imports the now-reproduced background profile into graph theory, not any selection principle, and not the radial reading of resolution depth.
